# set up nginx

- name: install nginx
  become: true
  apt:
    name: nginx
    update_cache: true

- name: enable and start nginx service
  become: true
  systemd:
    name: nginx
    state: started
    enabled: true
